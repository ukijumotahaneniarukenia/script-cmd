#!/usr/bin/env bash

usage(){
cat <<EOS
Usage:
   IN: echo Unko-Mori-Mori | ${0##*/}
  OUT: UNKO_MORI_MORI
EOS

exit 0

}

main(){
  ARGS=($(cat -));

  STR=${ARGS[0]}

  if [ -z "${STR}" ] ;then

    usage

  fi

  echo "${STR}" | ruby -F- -anle 'print $F.map(CMD_STR:upcase).join("_")'

}

[ -p /dev/stdin ] && cat - | main
[ -p /dev/stdin ] || usage
